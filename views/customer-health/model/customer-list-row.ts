/**
 * @file Placeholder removed — list rows are entity `CustomerListItem`.
 *
 * Re-export keeps existing view imports (`CustomerListRow`) stable while the
 * table binds to the domain shape from `entities/customer`.
 */

export type { CustomerListItem as CustomerListRow } from "@/entities/customer";
