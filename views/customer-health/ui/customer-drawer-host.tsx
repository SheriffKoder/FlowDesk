"use client";

/**
 * @file Client drawer host placeholder (open-first hook + panel land later).
 * Mounts at page level so a future panel can flush to the viewport edge.
 * Closed by default — no permanent chrome until a customer is selected.
 */

export function CustomerDrawerHost() {
  return null;
}
