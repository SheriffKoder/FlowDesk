/**
 * @file views/customer-health/ui/cards/placeholder/types.ts
 *
 * Purpose: Prop shapes for the empty overview card slot.
 */

export type PlaceholderCardProps = {
  className?: string;
  /** Optional a11y label when the slot is intentionally empty. */
  "aria-label"?: string;
};
