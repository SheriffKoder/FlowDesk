# Widgets

Composed reusable UI sections shared across views.

| Widget | Role |
|---|---|
| [`app-sidebar`](./app-sidebar) | Slim icon rail + mobile dock + layout `AppHeader` + `AppShell` |

Add a widget only when a composed block is reused (or is true app chrome) and would otherwise be duplicated in views.
