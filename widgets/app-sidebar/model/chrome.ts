/**
 * @file widgets/app-sidebar/model/chrome.ts
 *
 * Purpose: Shared layout chrome dimensions (header ↔ sidebar mark).
 * Used in: `AppHeader`, `AppSidebar` rail brand block.
 * Used for: Keep top bar and logo container the same height / feel.
 */

/** Height of layout header + sidebar logo container. */
export const APP_CHROME_BRAND_HEIGHT = "h-20";

/** Desktop icon rail width. */
export const APP_CHROME_RAIL_WIDTH = "w-20";
